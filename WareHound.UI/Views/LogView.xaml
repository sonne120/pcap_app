<UserControl x:Class="WareHound.UI.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource PrimaryBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="Application Logs" FontSize="20" FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="View and manage application log entries" 
                           FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            
            <!-- Action Buttons in Header -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource ModernButtonStyle}"
                        Content="Copy" Command="{Binding CopySelectedCommand}" 
                        Height="26" Padding="16,0" Margin="0,0,8,0"/>
                <Button Style="{StaticResource ModernButtonStyle}"
                        Content="Export" Command="{Binding ExportLogsCommand}" 
                        Height="26" Padding="16,0" Margin="0,0,8,0"/>
                <Button Style="{StaticResource ModernButtonStyle}"
                        Content="Clear" Command="{Binding ClearLogsCommand}" 
                        Height="26" Padding="16,0"/>
            </StackPanel>
        </Grid>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Filter by Level -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Level:" Foreground="{DynamicResource TextSecondaryBrush}" 
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Style="{StaticResource ModernComboBoxStyle}"
                          Width="100" Height="26"
                          ItemsSource="{Binding LogLevels}"
                          SelectedIndex="{Binding SelectedLevelIndex}"/>
            </StackPanel>

            <!-- Filter by Text with placeholder -->
            <Grid Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                <TextBlock Text="Search:" VerticalAlignment="Center" 
                           Foreground="{DynamicResource TextSecondaryBrush}" 
                           Margin="10,0,0,0" 
                           IsHitTestVisible="False"
                           Panel.ZIndex="1"
                           Visibility="{Binding FilterText, Converter={StaticResource NullToVisibility}, ConverterParameter=Invert}"/>
                <TextBox Style="{StaticResource ModernTextBoxStyle}"
                         Width="200" Height="26"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" Padding="8,0"/>
            </Grid>
        </Grid>

        <!-- Log List -->
        <Border Grid.Row="2" Background="{DynamicResource SecondaryBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <DataGrid x:Name="LogGrid"
                      ItemsSource="{Binding LogEntries}" 
                      SelectedItem="{Binding SelectedLogEntry}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      RowHeaderWidth="0"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      EnableRowVirtualization="True">
                
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Level}" Value="Error">
                                <Setter Property="Foreground" Value="#E74C3C"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Level}" Value="Warning">
                                <Setter Property="Foreground" Value="#F39C12"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Level}" Value="Debug">
                                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding TimestampDisplay}" Width="90"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding LevelDisplay}" Width="60"/>
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="{DynamicResource TertiaryBrush}" 
                CornerRadius="4" Margin="0,8,0,0" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Total:" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,4,0"/>
                    <TextBlock Text="{Binding LogCount}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="Medium"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="24,0,0,0">
                    <TextBlock Text="Errors:" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,4,0"/>
                    <TextBlock Text="{Binding ErrorCount}" Foreground="#E74C3C" FontWeight="Medium"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="24,0,0,0">
                    <TextBlock Text="Warnings:" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,4,0"/>
                    <TextBlock Text="{Binding WarningCount}" Foreground="#F39C12" FontWeight="Medium"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
