<UserControl x:Class="WareHound.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{StaticResource PrimaryBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" MinWidth="200"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Collections List (Left Panel) -->
        <Border Grid.Column="0" Background="{StaticResource SecondaryBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource TertiaryBrush}" Padding="12,10">
                    <TextBlock Text="Saved Collections" FontWeight="SemiBold" FontSize="14"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </Border>

                <ListBox Grid.Row="1" ItemsSource="{Binding Collections}" 
                         SelectedItem="{Binding SelectedCollection}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8,6" Margin="4,2" CornerRadius="4"
                                    Background="{StaticResource TertiaryBrush}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="{Binding Name}" 
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                                        <TextBlock Text="{Binding PacketCount, StringFormat='{}{0} packets'}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11"/>
                                        <TextBlock Text=" • " Foreground="{StaticResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:MM/dd HH:mm}'}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Export to CSV" Command="{Binding ExportCommand}"/>
                            <Separator/>
                            <MenuItem Header="Delete Collection" Command="{Binding DeleteCommand}"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                </ListBox>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

        <!-- Packet Details (Right Panel) -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" MinHeight="150"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="*" MinHeight="100"/>
            </Grid.RowDefinitions>

            <!-- Packet List -->
            <DataGrid x:Name="PacketGrid" Grid.Row="0" 
                      ItemsSource="{Binding SelectedCollection.Packets}" 
                      SelectedItem="{Binding SelectedPacket}"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{Binding Protocol, Converter={StaticResource ProtocolToColor}}"/>
                        <Setter Property="Foreground" Value="#1E1E1E"/>
                        <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="No." Binding="{Binding Number}" Width="60"/>
                    <DataGridTextColumn Header="Time" Binding="{Binding TimeDisplay}" Width="100"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding SourceIp}" Width="130"/>
                    <DataGridTextColumn Header="Destination" Binding="{Binding DestIp}" Width="130"/>
                    <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="70"/>
                    <DataGridTextColumn Header="Src Port" Binding="{Binding SourcePort}" Width="70"/>
                    <DataGridTextColumn Header="Dst Port" Binding="{Binding DestPort}" Width="70"/>
                    <DataGridTextColumn Header="Info" Binding="{Binding Info}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"/>

            <!-- Packet Details TreeView -->
            <Border Grid.Row="2" Background="{StaticResource PrimaryBrush}" 
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1">
                <TreeView ItemsSource="{Binding PacketDetails}"
                          Visibility="{Binding SelectedPacket, Converter={StaticResource NullToVisibility}}">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <TextBlock Text="{Binding Text}" FontFamily="Consolas"/>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>

            <!-- Empty State -->
            <TextBlock Grid.Row="0" Grid.RowSpan="3" 
                       Text="Select a collection to view packets"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="16"
                       Visibility="{Binding SelectedCollection, Converter={StaticResource NullToVisibility}, ConverterParameter=Invert}"/>
        </Grid>
    </Grid>
</UserControl>
